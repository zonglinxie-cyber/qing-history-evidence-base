# 贡献指南

欢迎参与清史证据库。本项目不是"文章更长的清史百科"，而是一套把人物、关系、事件、史料、画像和争议结论逐条连接到原始证据的知识基础设施。**贡献的第一标准是可追溯性，不是数量或文采。**

## 参与路径

| 路径 | 适合 | 入口 |
|---|---|---|
| 报错/纠错 | 发现事实错误、链接失效、图像认定或权利问题 | [Issues](https://github.com/zonglinxie-cyber/qing-history-evidence-base/issues/new/choose)，请按模板填写 |
| 提交修正 | 你能提供来源的修正、补全缺失字段、新增来源单元 | Pull Request（流程见下） |
| 认领任务 | 长期参与 | 见 `data/task-queue.csv`，先在 Issue 里认领 |

## 证据规则（不可妥协）

以下五条是项目锁定原则，所有贡献必须遵守：

1. **文章不是事实源。** 事实首先保存为带证据的原子主张（CSV 行），文章只是派生视图。
2. **关系边必须有证据。** 生母、嫡母、养母、承嗣、过继、婚配、政治关系不得混写。
3. **历史时间必须保留原值。** 年号纪年、闰月、公历换算、精度和换算方法同时保存。
4. **能浏览不等于能复制。** 图片、档案先进版权台账（`data/source-rights-ledger.csv`），再决定本地保存、外链或禁用。
5. **AI 没有发布权。** AI 只生成候选；使用 AI 辅助的贡献必须声明，并已完成人工核对。

补充硬规则：

- 每条主张必须能精确到**版本、卷页/档号/图像区域**；只给网址不给卷页的引用会被退回。
- "尚未找到材料"不得写成"没有发生"；缺证据的内容标 `审核中` 或进冲突组。
- 《清史稿》是民国清史馆所纂未定稿（证据等级 C），不得作为唯一事实来源；高风险事实须有 A1/A2 证据。
- 不提交红色/黄色资源的受限高清图；不绕过任何网站的访问控制。
- 不采集在世人物及其后裔的隐私信息。

## PR 流程

1. Fork 本仓库，在 `phase-0/` 目录工作。
2. 修改对应的 CSV / content/ 正文。新增主张请沿用现有 ID 段与列结构，状态从 `审核中` 起步，**不要自行改成「已采纳」**——采纳需要维护者抽查。
3. 本地验证：

   ```bash
   cd phase-0
   npm install
   npm run validate   # 必须全绿（0 errors）
   npm run build      # 重新生成 site/data/*.json
   ```

4. 提交 PR：说明你改了什么、依据哪条史料（版本+卷页）。CI 会自动运行 `validate` 与 `build`。
5. 维护者审核合并。涉及生母、承嗣、即位合法性、死因、画像认定的改动需要更高级别复核，可能需要等待。

## 数据格式速查

- 文件与必填列清单：`data/data-manifest.csv`（结构校验由它驱动）
- 主张的列规范与示例行：`data/kangxi-source-claims.csv` 表头 + `examples/assertion-example.json`
- 史料分级 A1/A2/B/C/D：`docs/03-source-and-rights-policy.md`
- 编辑规范全文：`docs/04-editorial-and-review-manual.md`
- 多朝代扩展：新朝代在 `data/dynasties.csv` 加一行 + 在 `data-manifest.csv` 登记文件，不改脚本

## 贡献许可

提交 PR 即表示你同意：

- 代码部分以 MIT 许可发布；
- 数据与文本内容以 CC BY 4.0 发布（见 LICENSE 与 LICENSE-data）;
- 你贡献的内容不侵犯第三方权利（引用受限材料请先看版权三色规则）。

## 不会被接受的贡献

- 无来源定位的事实主张，或只有 D 级来源（百科、自媒体、影视）的主张；
- 把流行叙事（"九子夺嫡""改诏夺位"等）当作已核事实写入；
- 未标明的 AI 生成文本、猜补的残字、无证据的死因与动机推断；
- 绕过版权台账的图片或整库抓取的数据；
- 绕过校验（validate）的改动。

谢谢你先读完这些再动手——这正是这个项目需要的人。
